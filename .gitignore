# Python - ไฟล์ระบบที่ไม่จำเป็น
__pycache__/
*.py[cod]
*$py.class
.venv/
env/
venv/
ENV/

# Data & Models - ปลดล็อกให้ไฟล์ขึ้น GitHub เพื่อให้ Render นำไปใช้งานได้
# ปิดการ ignore ไฟล์เหล่านี้โดยใส่เครื่องหมาย # ไว้ข้างหน้า
# *.csv
# *.pkl
# *.model

# OS - ไฟล์ระบบของคอมพิวเตอร์
.DS_Store
Thumbs.db